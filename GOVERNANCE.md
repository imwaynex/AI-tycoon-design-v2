# 專案管理規範

本文件規定這個倉庫怎麼變更、審查與發布。介面行為寫在 [SPEC.md](SPEC.md)，數值寫在 [tokens/](tokens/)，本文件只管流程。

## 1. 文件職責

| 檔案 | 職責 |
|---|---|
| `README.md` | 入口：這是什麼、先讀什麼、怎麼驗證。 |
| `SPEC.md` | 行為與約束。 |
| `tokens/` | 數值。`tokens/index.json` 另外記錄版本、狀態、全域設定與棄用登記。 |
| `GOVERNANCE.md` | 流程：變更分類、版本、棄用、提交、發布、寫作。 |
| `CHANGELOG.md` | 每個版本改了什麼，以及對消費者的影響。 |
| `scripts/check.py` | 把上述規則中能機器判斷的部分變成檢查。 |
| `AGENTS.md` | Agent 工作契約：讀取來源、修改、決策、驗證與交付的執行要求。 |
| `CLAUDE.md` | Claude Code 入口，匯入 `AGENTS.md`；共同規則只維護在 `AGENTS.md`。 |

`SPEC.md` 與 `tokens/` 互相矛盾時，視為缺陷，修正前不得發布。兩者不分主從：修正時同時檢查兩邊，不以其中一方覆蓋另一方。

## 2. 角色

- **維護者**：接受或退回變更，決定版本號，執行發布。目前由倉庫擁有者擔任。
- **貢獻者**：提出並實作變更。維護者自己改動時也是貢獻者，走同一套流程。
- **消費者**：頁面、元件與主題的實作者。消費者只依賴已發布的版本標籤，不依賴 `main` 上還沒發布的內容。

只有一位維護者時，由維護者依第 5 節的清單自審。

## 3. 變更分類

每一個變更都歸入下列其中一類。一次變更含有多類時，取影響最大的一類。

| 類別 | 內容 |
|---|---|
| 不相容 | 刪除或改名 token。改變 `breakpoint`、`shortHeight`、`layout.container` 的數值。改變 `layer.z` 的順序。改變 token 格式、單位或引用語法。收緊規則，使原本合規的實作變成不合規。 |
| 新增 | 新增 token、檔案或規則，而且既有實作不需要修改。改變一般 token 的數值，例如色值、間距、時長；介面名稱不變，但畫面會改變。 |
| 修正 | 不改變意思的文字澄清、錯字、排版。驗證腳本的修正。 |

## 4. 版本

版本號寫在 `tokens/index.json` 的 `version`，格式遵循語意化版本 `主.次.修`。

| 類別 | 1.0.0 之前 | 1.0.0 之後 |
|---|---|---|
| 不相容 | 次版本 +1 | 主版本 +1 |
| 新增 | 修訂版本 +1 | 次版本 +1 |
| 修正 | 修訂版本 +1 | 修訂版本 +1 |

`status` 跟著主版本走：主版本為 0 時是 `foundation`，1 以上是 `stable`。第一個正式的頁面或元件開始消費本倉之前，發布 1.0.0。

版本號只在發布時遞增。還沒發布的變更記在 `CHANGELOG.md` 最上方的 `## 未發布` 段落。

## 5. 變更流程

1. 從 `main` 開分支，命名為 `<範圍>/<簡述>`，例如 `spec/dialog-sm`。範圍見第 7 節。
2. 修改。同一件事牽涉的 `SPEC.md` 條款與 token，放在同一個提交裡一起改，不讓任何一個提交停在一半的狀態。
3. 執行 `python3 scripts/check.py`。啟用 pre-commit hook 後，提交時會自動執行。
4. 在 `CHANGELOG.md` 的 `## 未發布` 底下記錄變更，放進第 3 節對應類別的小標題。
5. 依下列清單自審，再合併回 `main`。`main` 上的每一個提交都必須通過檢查。

自審清單：

- [ ] `scripts/check.py` 通過。
- [ ] 變更類別判斷正確；不相容變更在 CHANGELOG 寫明消費者要怎麼改。
- [ ] 改名或刪除的 token 已處理棄用，或是在 1.0.0 之前直接列為不相容。
- [ ] `SPEC.md` 中提到被改動 token 的條款都已同步。
- [ ] 新的前景與底色組合已加入 `color.contrast`。
- [ ] 新 token 在 `SPEC.md` 寫了用途。
- [ ] 用詞符合第 8 節。

## 6. 棄用

1.0.0 之前，可以直接改名或刪除 token，在 CHANGELOG 列為不相容。

1.0.0 之後，改名或刪除要先棄用，至少保留到下一個主版本：

1. 舊 token 保留，值改為引用新 token。沒有替代品時，保留原值。
2. 在 `tokens/index.json` 的 `deprecated` 登記：

   ```json
   "deprecated": {
     "color.semantic.text.muted": {
       "replacedBy": "color.semantic.text.tertiary",
       "since": "1.2.0",
       "removeIn": "2.0.0"
     }
   }
   ```

   沒有替代品時，`replacedBy` 寫 `null`。
3. 在 CHANGELOG 的「新增」類別下記錄棄用。
4. 到達 `removeIn` 的版本時刪除舊 token 與登記，列為不相容。

`scripts/check.py` 會確認登記的 token 存在、確實引用 `replacedBy`，並在版本到達 `removeIn` 時報錯，提醒刪除。

## 7. 提交與發布

提交訊息的第一行寫成 `<範圍>：<摘要>`，摘要用一句話說明做了什麼，不超過 50 字。需要說明原因時，空一行寫在內文。

| 範圍 | 用於 |
|---|---|
| `spec` | 行為規則，通常同時改到 `SPEC.md` 與 `tokens/`。 |
| `tokens` | 只改數值，不改規則。 |
| `scripts` | 驗證工具。 |
| `docs` | `README.md`、`GOVERNANCE.md`、`AGENTS.md`、`CLAUDE.md` 與不改變設計規則的文字修正。 |
| `release` | 發布提交。 |

發布步驟：

1. 確認 `main` 通過檢查，工作區乾淨。
2. 依第 3、4 節決定新版本號，更新 `tokens/index.json` 的 `version`，必要時更新 `status`。
3. 把 CHANGELOG 的 `## 未發布` 改成 `## [主.次.修] - YYYY-MM-DD`。
4. 提交 `release：主.次.修`。
5. 打附註標籤：`git tag -a v主.次.修 -m "主.次.修"`。

## 8. 寫作

**語言**：使用繁體中文與台灣用語。下列詞語由 `scripts/check.py` 檢查，被反引號包住的引用不在檢查範圍：

| 使用 | 不使用 |
|---|---|
| 全螢幕 | `全屏` |
| 螢幕 | `屏幕` |
| 支援 | `支持` |
| 導覽 | `導航` |
| 預設 | `默認` |
| 資訊 | `信息` |
| 設定 | `設置` |
| 文件 | `文檔` |
| 資料 | `數據` |
| 影片 | `視頻` |
| 介面 | `接口` |
| 使用者 | `用戶` |
| 實作 | `實現` |
| 相容 | `兼容` |
| 選單 | `菜單` |
| 滑鼠 | `鼠標` |
| 載入 | `加載` |
| 模組 | `模塊` |
| 元件 | `組件` |

**規範句**：`SPEC.md` 的每一條都寫成可以驗證的要求，使用「使用」「不得」「至少」「不超過」這類詞。不使用 `盡量`、`盡可能`、`建議`、`酌情`；這些詞也由檢查腳本攔截。還沒決定的事項寫進「此階段不包含」，不寫成模糊的條款。

**token 名稱**：用反引號包住，寫成路徑的一段連續片段，例如 `target.coarse`、`radius.usage.dialog`。檢查腳本靠這個判斷 token 是否有規則；`sm`、`primitive` 這類泛用詞單獨出現時不算數。

**數值**：寫出單位與比例，例如 44px、4.5:1、200%。

## 9. 自動化

`.githooks/pre-commit` 會在每次提交前執行 `scripts/check.py`。複製倉庫後啟用一次：

```sh
git config core.hooksPath .githooks
```

之後設定遠端時，在 CI 執行同一支腳本，不另寫一套規則。
