# Agent 工作契約

本文件適用於在 `design_v2` 工作的 Agent。共同規則維護在此，`CLAUDE.md` 匯入本文件。

## 1. 任務與範圍

維護響應式設計系統底層，讓介面行為、token 數值、文件與驗證工具保持一致。

目前範圍是規範、token、驗證工具與維護流程。品牌主題、具體畫面、業務流程及元件實作，只有在使用者明確要求時才展開，並同步處理現有文件中的階段範圍。後續實作沿用本倉的解析順序與 token 分層。

## 2. 資料來源與衝突處理

| 來源 | 用途 |
|---|---|
| [README.md](README.md) | 專案入口、閱讀順序與驗證方式。 |
| [SPEC.md](SPEC.md) | 介面行為、約束與 token 用途。 |
| [tokens/](tokens/) | 數值與引用；`tokens/index.json` 記錄版本、狀態、全域設定與棄用登記。 |
| [GOVERNANCE.md](GOVERNANCE.md) | 變更分類、版本、棄用、提交、發布與寫作規則。 |
| [CHANGELOG.md](CHANGELOG.md) | 已發布與未發布的變更及消費者影響。 |
| [scripts/check.py](scripts/check.py) | 可以由程式判斷的檢查。 |

`SPEC.md` 與 `tokens/` 共同成立，兩者不分主從。發現矛盾時，列出相關條款、數值與引用，依任務要求和現有依據一起修正，不任選一方覆蓋另一方。現有資料無法判定原意時，提出具體選項與影響，交由維護者決定。

檢查腳本是驗證工具；腳本與規範不一致時，釐清原因並修正，不以腳本目前接受的內容反推設計決策。

## 3. 開始工作的步驟

1. 先讀 `README.md`、`GOVERNANCE.md`，再讀任務涉及的 `SPEC.md` 條款、`tokens/index.json` 與相關 token 檔案。
2. 使用 `git status --short` 與相關檔案的差異確認工作區狀態。保留既有修改，不回復、清除或覆寫使用者尚未提交的工作。
3. 搜尋被修改名稱的定義與引用，確認規範、token、驗證工具及變更紀錄的影響範圍。
4. 依 `GOVERNANCE.md` 判斷變更類別與分支流程。工作區已有進行中的修改時，先辨識所屬任務，不為整理分支而搬移或丟棄既有工作。
5. 簡述要改的內容與驗證方式，再完成任務範圍內的修改。

## 4. 修改時必須一起完成的事

| 改動 | 同步工作 |
|---|---|
| 新增 token | 在 `SPEC.md` 定義用途；新增 token 檔案時，同時更新 `tokens/index.json` 的 `files`。 |
| 修改 token 名稱或數值 | 搜尋並更新受影響的規範條款、引用與範例，確認數值格式、單位及引用語法一致。 |
| 修改原始 token | 追查直接與間接引用它的語意 token，確認其用途仍成立。 |
| 修改視窗或容器斷點 | 檢查受影響的寬度帶、容器帶、按帶取值的語意 token 與結構切換規則。 |
| 修改色彩或新增前景與底色組合 | 檢查受影響的 `color.contrast` 配對；新增實際使用的組合時，同時加入對比檢查。 |
| 改名或刪除 token | 依 `GOVERNANCE.md` 處理相容性與棄用，並在變更紀錄說明消費者要怎麼改。 |
| 修改可機器驗證的規則 | 檢查 `scripts/check.py` 是否需要同步；不得只為消除錯誤而降低門檻或移除檢查。 |
| 新增或調整文件職責 | 同步 `README.md` 與 `GOVERNANCE.md` 的文件索引。 |

同一件事涉及的規範、token 與驗證工具，在同一次變更中完成；提交時遵守 `GOVERNANCE.md` 的同步要求。

每次變更依分類記入 `CHANGELOG.md` 最上方的 `## 未發布`，沒有該段落時新增。保留既有紀錄；不相容變更寫明遷移方式。版本號只在發布時調整，規則依 `GOVERNANCE.md`，不在日常修改時自行遞增。

## 5. 自主決策與提問原則

已授權範圍內的必要修改、文件同步與驗證，直接完成。現有規範足以判斷的細節，自行處理並交代依據，不重複詢問已獲授權的事項。

缺少資訊會改變需求、相容性或任務範圍時，提出具體問題、選項與影響。等待回答期間，繼續不依賴答案的工作；未決事項不得寫成已確認規則。

任務外的問題先記錄影響，不自行擴大成無關重構。發布由維護者決定；使用者已明確授權代為發布時，依 `GOVERNANCE.md` 完成流程，否則交付可供審查的修改。

## 6. 完成標準

修改後，在倉庫根目錄執行：

```sh
python3 scripts/check.py
```

此指令只需要 Python 3 標準函式庫。修正本次變更造成的失敗，通過後再交付；既有且與任務無關的失敗，說明位置、影響及未處理原因。

交付前人工確認：

- 修改符合任務要求，規範、數值、引用與範例沒有互相矛盾。
- 新 token 的用途已定義，相容性、棄用與變更紀錄已依影響處理。
- 檢查本次差異，確認沒有混入無關修改或破壞使用者既有工作。
- 修改驗證腳本時，以能呈現問題的案例確認修正有效，並確認正常資料仍能通過。

腳本通過只代表其涵蓋的檢查通過。目前本倉沒有畫面或元件實作，不得據此宣稱已驗證實際響應式行為、鍵盤操作或完整無障礙體驗。沒有執行、無法執行及仍待實作驗證的項目，分別如實交代。

## 7. 交付與文件維護

使用繁體中文與台灣用語，寫作細節依 `GOVERNANCE.md`。規範句寫成可驗證的要求；token 名稱用反引號包住，數值附上適用的單位或比例。

交付時簡述改了什麼、原因、驗證結果與尚未解決的事項，附上相關檔案位置。只有實際執行並取得結果的檢查，才能回報為通過。

本文件只保留長期有效的工作規則。版本與數值查各自來源，歷史變更放在 `CHANGELOG.md`；不把一次性任務進度或對話逐字紀錄寫進本文件。

共同規則的修改集中在 `AGENTS.md`。`CLAUDE.md` 保留匯入與必要的 Claude Code 專用說明；調整時確認匯入路徑存在，且沒有重複或互相矛盾的指令。
