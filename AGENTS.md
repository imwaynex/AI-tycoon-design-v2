# Agent 工作契約

適用於整個倉庫。共同執行規則只維護在本檔；`CLAUDE.md` 匯入本檔，不建立另一套契約。

## 1. 任務與範圍

維護規範、token、Hub 文件與參考原型、驗證工具及流程的一致性。現況與任務入口見 [README.md](README.md)。沒有明確授權，不擴充成產品程式、城市建築方案、業務流程、品牌主題或發布工作。

## 2. 最小閱讀集合

先讀本檔及 [管理規範](GOVERNANCE.md) 的文件分工、變更流程與寫作規則，再依 README 的任務路由讀取相關來源。完整角色登記見 [docs/index.json](docs/index.json)。

涉及 Hub 時，先讀 [Hub 入口](docs/hub/README.md) 與正式規範；只在需要互動細節時讀原型。涉及 token 時，讀 `tokens/index.json`、受影響的檔案、`SPEC.md` 用途與直接／間接引用。不要預先載入無關原型或把整份歷史紀錄當成現行規則。

## 3. 工作區與遠端安全

本地工作先執行 `git status --short`、確認目前分支，再檢查任務相關差異。保留使用者既有修改；不得用重設、清除或強制推送整理工作區。

只有 API／連接器時，先確認預設分支、相關 PR 與固定 commit，再讀該版本的檔案；不得把不同時間的讀取拼成同一個快照。更新既有檔案須帶目前 blob SHA；多檔相關變更使用同一棵 tree 與單一 commit。寫入前確認目標分支未移動，不覆蓋他人新增的提交。

依管理規範開工作分支、提交並建立 PR；未獲授權不自行合併、打標籤或發布。不要在工具輸出、文件或提交中寫入憑證與私人環境資訊。

## 4. 決策與衝突

依 [管理規範第 1 節](GOVERNANCE.md#1-文件職責) 判斷來源職責，不使用「檔名較新／版本較大／離程式較近」作為優先理由。規範、原型、歷史紀錄與驗證結果必須分開。

已授權且來源明確的必要修正直接完成。來源不足時，記錄衝突雙方的路徑、條款、影響、待決內容與決策者；只暫停依賴該決策的部分，繼續其他工作。不得把推測或尚未測試的結果寫成已確認；不得從原型補完正式規範刻意排除的城市或業務設計。

## 5. 修改與同步

同一需求的規範、token、驗證與變更紀錄一起修改，不混入無關重構。

| 改動 | 必須追查 |
|---|---|
| token 新增／改名／改值 | `SPEC.md` 用途、引用鏈、受影響頁面、版本與棄用規則；新增檔案同步 token 索引。 |
| 斷點、容器、層級或對比 | 對應規範、語意引用、檢查資料與兩種方向下的行為。 |
| 文件新增／改名／改角色 | `docs/index.json`、所有內部連結與任務入口；不複製另一份規範。 |
| 原型更新 | 保留參考身分，更新來源完整性登記與差異說明；不得因此宣稱驗收通過。 |
| 驗證規則修改 | 新增會失敗的反例與正常案例；不得降低門檻來消除錯誤。 |

每次變更記入 `CHANGELOG.md` 的「未發布」並保留既有紀錄；不在日常任務中遞增發布版本。提交、分類與相容性依管理規範。

## 6. 驗證與完成標準

在倉庫根目錄執行：

```sh
python3 scripts/check.py
python3 -m unittest discover -s scripts -p 'test_*.py'
git diff --check
```

交付前逐項檢查差異：有無修改定稿含義、留下衝突、混入無關檔案，以及需要產品或實機驗證的項目是否標明。

無法執行命令時，明確回報「未執行」、原因與所做的靜態檢查；不得用自行重寫的部分檢查冒充上述指令。若以連接器檔案建立本地驗證快照，核對其來源 commit 與檔案雜湊，再執行原本的檢查程式。

## 7. 交付與維護

使用繁體中文與台灣用語。交付內容包括修改目的、檔案位置、實際執行的命令與結果、未決事項、分支／commit／PR，以及是否已合併。只把真正執行成功的項目標為通過。

自動檢查不等同語意衝突審查，也不等同實機 RWD、鍵盤、焦點或無障礙驗收。Agent 契約不保存一次性進度、測試數量、設計數值或版本；這些內容留在各自來源與 PR。
