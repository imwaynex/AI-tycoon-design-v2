# RWD 設計系統底層規範

版本見 `tokens/index.json`。本文件定義行為，`tokens/` 定義數值。任一 token 被改名、改值或新增斷點時，同步更新本文件中引用它的條款，並遞增版本。

本倉此階段只建立底層。品牌主題、個別畫面與元件程式都還沒有。後續畫面必須消費這裡的語意 token 與佈局規則。

## 1. 模型

介面只有一份。寬度、高度、容器寬度、指標類型與使用者偏好共同決定當下的佈局。不同尺寸的靜態稿是同一份規則的檢查點，每張稿都要能從規則推出。

變化分兩種：

- **流動**發生在兩個寬度帶之間。間距、字級、欄寬隨可用空間連續變化。
- **結構切換**只發生在命名斷點或短高度條件成立時。導航所在區域、欄數、對話框是全屏還是居中面板，屬於結構切換。

基準樣式服務最窄的連續寬度。較寬的寬度帶只增加或替換結構。輸入方式與寬度帶是兩條軸：寬螢幕可以是觸控，窄視窗可以是滑鼠。懸停、粗指標、鍵盤各自作用，不綁死在某個寬度帶上。

解析順序固定為：

1. 讀取環境：視窗寬度、視窗高度、安全區、指標粗細、是否可懸停、系統文字縮放、是否要求減少動效。
2. 用寬度選定寬度帶：小於 `sm` 為 `base`，小於 `md` 為 `sm`，小於 `lg` 為 `md`，其餘為 `lg`。
3. 高度小於 `shortHeight` 時啟用短高度規則，並把密度設為 compact。寬度帶維持不變。
4. 取出該寬度帶的語意 token。
5. 頁面模板放置區域。
6. 宣告了容器規則的元件，依自身內容盒寬度選容器帶，再排內部。
7. 互動狀態只改變外觀或取代內容，不重新選擇寬度帶。

## 2. 寬度帶、容器與高度

視窗斷點使用 min-width，數值在 `tokens/layout.json` 的 `breakpoint`。

| 寬度帶 | 條件 | 欄數 |
|---|---|---|
| `base` | 寬度小於 `sm` | 4 |
| `sm` | 大於等於 `sm`，小於 `md` | 8 |
| `md` | 大於等於 `md`，小於 `lg` | 12 |
| `lg` | 大於等於 `lg` | 12 |

支援的最小寬度是 `minWidth`（320px）。頁面在這個寬度、以及正文放大到 200% 時，本身不產生橫向捲動。資料表這類本身更寬的區域可以在區域內橫向捲動，頁面其他部分保持不動。

容器斷點在 `layout.container`，只作用於元件內部：

| 容器帶 | 條件 |
|---|---|
| 預設 | 內容盒寬度小於 `container.sm` |
| `c-sm` | 大於等於 `container.sm`，小於 `container.md` |
| `c-md` | 大於等於 `container.md` |

頁面模板看視窗。會在窄欄裡出現的元件看容器。元件沒有容器規則時，使用它的窄容器佈局，讓它在任何欄寬都能成立。

短高度（`shortHeight`，500px）追加這些規則：密度使用 compact；次要操作收進溢位；主要操作保持可見；同時最多一條黏性條。短高度不另建一套介面。

方向只是寬度與高度的結果。橫放的窄視窗若寬度進入 `sm` 且高度仍短於 `shortHeight`，使用 `sm` 的欄數，並套用短高度規則。

## 3. 佈局

預設佈局是流式容器：直向堆疊、橫向列、格線。區域的位置用欄距與對齊表達。

只有貼著視窗邊緣、且必須在捲動時留在原位的介面飾條可以使用邊緣錨定。錨定元素放在安全區之內。佔流的黏性區域保留自己的高度。脫離流動的固定層必須用同等 token 把內容推開，避免遮住標題或主要操作。安全區只加一次，加在頁面外緣，不在每個元件上再加。

格線使用當前寬度帶的 `columns`、`margin` 與 `gutter`。頁面正文區域寬度不超過 `contentMax`，需要更寬工作區的模板不超過 `wideMax`，超出部分留白。長文一行不超過 `measure`。區域之間的距離使用 `regionGap`。

`100vw` 不拿來設定頁面寬度，以免捲軸寬度造成橫向溢出。可用寬度以內容盒為準。

頁面模板為每個寬度帶宣告區域的順序、跨越欄數，以及是否黏性。視覺順序與閱讀順序一致。模板可以在斷點把一個區域搬到側邊，但搬移必須寫在模板裡，元件不得自行用絕對定位換位。

對話框在 `base` 與短高度時佔滿視窗，內部捲動。從 `md` 起改為居中面板，寬度不超過 `dialogMax`。標題、關閉與主要操作在兩種形式裡都保持可達。

## 4. 間距、尺寸與字級

間距只使用 `tokens/space.json` 的級距。級距從 4px 起跳，數值是設計像素。實作時以 `rootPx` 16 為基準換成 rem，讓系統文字縮放能帶動間距。下列項目維持 px：點擊目標、1px 與 2px 邊框、焦點環。

點擊目標見 `tokens/size.json`：

- 粗指標最小邊長 `target.coarse`（44px）。
- 細指標最小邊長 `target.fine`（32px）。
- 相鄰目標間距至少 `targetGap`。
- 視覺圖示可以小於目標，目標由元件的可點範圍完成。

控制項高度使用 `control.regular`，除非該元件明確選了 compact 或 comfortable。短高度與 compact 密度才使用 `control.compact`，而且粗指標下仍不得小於 `target.coarse`。

字級角色在 `tokens/type.json`。`display`、`title`、`heading` 在帶與帶之間流動，表達式同時包含 rem 與 vw，避免只有 vw 時使用者放大文字失效。`body` 固定 1rem。`caption` 是最小文字，不得再小。正文與說明的最小設計像素見 `floorPx`。數字與連續拉丁文使用同一套文字角色；等寬字只用在需要對齊的程式或原始資料。

放大到 200% 時，容器跟著內容長高，文字保持可見。用截斷省略來換取單行時，完整內容要有其他到達方式。

## 5. Token

Token 分三層：

| 層 | 內容 | 誰可以引用 |
|---|---|---|
| 原始 | 沒有用途的級距，例如空間級距、色板 | 語意層 |
| 語意 | 有用途的決策，例如頁面邊距、正文、畫布底色 | 模板與元件 |
| 元件 | 某個元件必須偏離語意值時的別名 | 該元件 |

模板與元件引用語意 token。它們不寫原始色碼、不寫未經 token 的像素、不在斷點旁抄一份魔術數字。元件 token 必須指回語意 token。

引用語法是 `{檔案id.物件路徑}`。`color.semantic.text.primary` 這類路徑裡，每一段都是物件鍵，不使用把多個詞拼在同一個鍵裡的點號。

一個語意 token 可以按寬度帶給值，例如 `layout.margin`。消費端只寫 token 名稱，由解析順序選值。`base` 是缺省值；較寬的帶沒有寫值時，沿用下一個較窄的帶。

改原始 token 時，檢查所有指到它的語意 token。改斷點時，檢查每一個按寬度帶取值的語意 token。改完遞增 `tokens/index.json` 的 `version`。

## 6. 色彩與對比

`tokens/color.json` 的 `primitive` 是結構用色板，`semantic` 是介面實際使用的角色。主題是另一份語意指向，用來替換 `semantic` 的目標；主題不改元件規則，也不改斷點。本階段只提供一份預設指向。

對比要求寫在 `color.contrast`，單位是 WCAG 相對對比：

- 主要文字對畫布、表面至少 7:1。
- 次要文字、說明文字、狀態文字對其所在底色至少 4.5:1。
- 強調色上的文字至少 4.5:1。

狀態不只靠顏色。成功、警告、危險、資訊同時使用文字或圖形。焦點使用 `focus.ring`，寬度與偏移見 `size.focus`，並且使用 `:focus-visible` 的時機，避免滑鼠點擊留下永遠存在的環。

## 7. 元件契約

本階段不製作元件庫。之後每一個元件都要符合這份契約。

元件宣告內容槽、尺寸（compact、regular、comfortable）、是否依容器改變內部結構，以及它支持的狀態。狀態集合是：預設、懸停、按下、鍵盤焦點、停用、忙碌、錯誤。沒有該互動的元件可以省略不適用的狀態。

外觀約束：

- 懸停與焦點不改變元件佔位。
- 錯誤文案參與流動，把後續內容往下推。
- 停用的控制項不可聚焦、不可提交。
- 忙碌的控制項保持聚焦，忽略重複啟動，並以文字或狀態呈現忙碌。
- 只在懸停時出現的操作，必須有不依賴懸停的到達方式。
- 截斷是元件的顯式選項，預設隨內容擴展。

元件內部若會從單欄變成多欄，必須寫容器帶，而不是讀視窗寬度。

## 8. 重排與輸入

寬度帶或短高度條件變化時，保留使用者已經產生的狀態：捲動位置、選取、輸入中的內容、展開中的對話框。重排不重複提交動作。

閱讀順序就是結構順序。寬度帶改變後，Tab 順序跟著新的視覺順序，兩者保持同一套順序。每個可互動表面在出現時有一個初始焦點位置；對話框把焦點放進自身，關閉後焦點回到開啟它的控制項。

鍵盤可以到達每一個操作。Enter 與 Space 的啟動行為與點擊相同。Escape 關閉最上層的暫時層。指標粗細改變時，目標尺寸立刻改用對應的 `target` 值。

軟鍵盤出現時，正在編輯的欄位與它的提交動作留在可見範圍內。欄位聚焦期間，單鍵捷徑不攔截文字輸入。

## 9. 層級與動效

疊層只使用 `tokens/layer.json` 的命名層。元件不自訂插入中間的 z 值。同一層內的新層蓋住舊層。對話框所在的 `modal` 高於下拉與遮罩；暫時訊息使用 `toast`，不蓋住需要立即決定的對話框。

海拔見 `tokens/elevation.json`。平坦表面用 `flat`，抬起的卡片用 `raised`，下拉用 `overlay`，對話框用 `dialog`。海拔表達層次，不拿來傳達狀態。

動效見 `tokens/motion.json`。結構切換使用 `instant` 或 `fast`，完成前不阻擋輸入。位移只用於表達進出的暫時層。使用者要求減少動效時，時長改為 `reduced.durationMs`；允許保留不造成位移的透明度變化。

## 10. 此階段不包含

- 品牌主題與第二套語意指向。
- 任何具體頁面、導航資訊架構或業務流程。
- 元件的視覺稿與程式實作。
- 國際化文案。規範只要求容器能隨文案變長，不在此凍結語言。

這些項目開始時，沿用本文件的解析順序與 token 分層，不平行再定義一套斷點。
