# RWD 設計系統底層規範

版本見 `tokens/index.json`。本文件定義行為，`tokens/` 定義數值，變更流程與版本規則見 [GOVERNANCE.md](GOVERNANCE.md)。任一 token 被改名、改值或新增斷點時，同步更新本文件中引用它的條款。

本倉此階段只建立底層。品牌主題、個別畫面與元件程式都還沒有。後續畫面必須消費這裡的語意 token 與佈局規則。

## 1. 模型

介面只有一份。寬度、高度、容器寬度、指標類型與使用者偏好共同決定當下的佈局。不同尺寸的靜態稿是同一份規則的檢查點，每張稿都要能從規則推出。

變化分兩種：

- **流動**發生在兩個寬度帶之間。間距、字級、欄寬隨可用空間連續變化。
- **結構切換**只發生在命名斷點或短高度條件成立時。導覽所在區域、欄數、對話框是全螢幕還是居中面板，屬於結構切換。

基準樣式服務最窄的連續寬度。較寬的寬度帶只增加或替換結構。輸入方式與寬度帶是兩條軸：寬螢幕可以是觸控，窄視窗可以是滑鼠。懸停、粗指標、鍵盤各自作用，不綁死在某個寬度帶上。

解析順序固定為：

1. 讀取環境：視窗寬度、視窗高度、安全區、指標粗細、是否可懸停、系統文字縮放、是否要求減少動效、是否要求更高對比、是否處於強制色彩模式。
2. 用寬度選定寬度帶：小於 `sm` 為 `base`，小於 `md` 為 `sm`，小於 `lg` 為 `md`，其餘為 `lg`。
3. 高度小於 `shortHeight` 時啟用短高度規則，並把密度設為 compact。寬度帶維持不變。
4. 取出該寬度帶的語意 token。
5. 頁面模板放置區域。
6. 宣告了容器規則的元件，依自身內容盒寬度選容器帶，再排內部。
7. 互動狀態只改變外觀或取代內容，不重新選擇寬度帶。

## 2. 寬度帶、容器與高度

視窗斷點使用 min-width，數值在 `tokens/layout.json` 的 `breakpoint`。實作時把斷點除以 `rootPx` 換成 em，例如 600px 寫成 37.5em。這樣系統放大預設文字時，寬度帶會跟著降檔，不會用寬螢幕的欄數去排放大後的字。容器斷點同樣換成 em。

| 寬度帶 | 條件 | 欄數 |
|---|---|---|
| `base` | 寬度小於 `sm` | 4 |
| `sm` | 大於等於 `sm`，小於 `md` | 8 |
| `md` | 大於等於 `md`，小於 `lg` | 12 |
| `lg` | 大於等於 `lg` | 12 |

支援的最小寬度是 `minWidth`（320px）。頁面在這個寬度、以及正文放大到 200% 時，本身不產生橫向捲動。資料表這類本身更寬的區域可以在區域內橫向捲動，頁面其他部分保持不動。

容器斷點在 `layout.container`，只作用於元件內部：

| 容器帶 | 條件 |
|---|---|
| 預設 | 內容盒寬度小於 `container.sm` |
| `c-sm` | 大於等於 `container.sm`，小於 `container.md` |
| `c-md` | 大於等於 `container.md` |

頁面模板看視窗。會在窄欄裡出現的元件看容器。元件沒有容器規則時，使用它的窄容器佈局，讓它在任何欄寬都能成立。

短高度（`shortHeight`，500px）追加這些規則：密度使用 compact；次要操作收進溢位；主要操作保持可見；同時最多一條黏性條。短高度不另建一套介面。

方向只是寬度與高度的結果。橫放的窄視窗若寬度進入 `sm` 且高度仍短於 `shortHeight`，使用 `sm` 的欄數，並套用短高度規則。

## 3. 佈局

預設佈局是流式容器：直向堆疊、橫向列、格線。區域的位置用欄距與對齊表達。

只有貼著視窗邊緣、且必須在捲動時留在原位的介面飾條可以使用邊緣錨定。錨定元素放在安全區之內。佔流的黏性區域保留自己的高度。脫離流動的固定層必須用同等 token 把內容推開，避免遮住標題或主要操作。安全區只加一次，加在頁面外緣，不在每個元件上再加。

格線使用當前寬度帶的 `columns`、`margin` 與 `gutter`。頁面正文區域寬度不超過 `contentMax`，需要更寬工作區的模板不超過 `wideMax`，超出部分留白。長文一行不超過 `measure`。區域之間的距離使用 `regionGap`。

`100vw` 不拿來設定頁面寬度，以免捲軸寬度造成橫向溢出。可用寬度以內容盒為準。

頁面模板為每個寬度帶宣告區域的順序、跨越欄數，以及是否黏性。視覺順序與閱讀順序一致。模板可以在斷點把一個區域搬到側邊，但搬移必須寫在模板裡，元件不得自行用絕對定位換位。

對話框在 `base` 與短高度時佔滿視窗，內部捲動，不帶圓角與陰影。其餘情況，也就是從 `sm` 起且高度不短，改為居中面板，寬度不超過 `dialogMax`。標題、關閉與主要操作在兩種形式裡都保持可達。

## 4. 間距、尺寸、字級與圓角

間距只使用 `space.scale` 的級距。級距以 `space.base`（4px）為單位，數值是設計像素。實作時以 `rootPx` 16 為基準換成 rem，讓系統文字縮放能帶動間距。下列項目維持 px：點擊目標、`size.border` 的邊框寬度、焦點環。

點擊目標見 `tokens/size.json`：

- 粗指標最小邊長 `target.coarse`（44px）。
- 細指標最小邊長 `target.fine`（32px）。
- comfortable 尺寸的元件，不分指標，最小邊長 `target.comfortable`（48px）。
- 相鄰目標間距至少 `targetGap`。
- 視覺圖示可以小於目標，目標由元件的可點範圍完成。

控制項高度依元件尺寸取 `control.compact`、`control.regular` 或 `control.comfortable`，未指定時使用 `control.regular`。短高度與 compact 密度才使用 `control.compact`，而且粗指標下仍不得小於 `target.coarse`。

因此在粗指標下，compact 不會讓控制項變矮，它的效果來自留白：元件內距與 `regionGap` 各往下取一個 `space.scale` 級距，最低到 `space.scale.1`。細指標下兩者同時生效。

圖示尺寸使用 `size.icon`，依同行文字或所在的控制項配對：

- `icon.sm` 配 `caption` 與 `label`。
- `icon.md` 配 `body`，以及 compact、regular 控制項。
- `icon.lg` 配 `heading` 以上，以及 comfortable 控制項。

圖示和間距一樣換成 rem，隨文字縮放；它的點擊範圍仍由目標尺寸決定。

邊框寬度使用 `size.border`。`size.border.hairline` 用於一般邊界與分隔，`size.border.strong` 用於選取、錯誤這類需要加強的狀態。加粗的邊框以內嵌描邊或陰影繪製，不改變元件佔位。

字級角色在 `tokens/type.json`。`display`、`title`、`heading` 在帶與帶之間流動，表達式同時包含 rem 與 vw，避免只有 vw 時使用者放大文字失效。`body` 固定 1rem。`label` 用於表單標籤、按鈕文字與表頭，不用於段落。`caption` 是最小文字，不得再小。正文與說明的最小設計像素見 `floorPx`。數字與連續拉丁文使用同一套文字角色；等寬字（`type.family.mono`）只用在需要對齊的程式或原始資料。

放大到 200% 時，容器跟著內容長高，文字保持可見。用截斷省略來換取單行時，完整內容要有其他到達方式。

圓角使用 `radius.usage`：控制項用 `radius.usage.control`，卡片與區塊容器用 `radius.usage.container`，居中對話框用 `radius.usage.dialog`，膠囊形的標籤與開關用 `radius.usage.pill`。貼齊視窗邊緣的表面，例如全螢幕對話框、頁首與頁尾，使用 `radius.scale.none`。巢狀時，內層圓角不大於外層。

## 5. Token

Token 分三層：

| 層 | 內容 | 誰可以引用 |
|---|---|---|
| 原始 | 沒有用途的級距，例如空間級距、色板 | 語意層 |
| 語意 | 有用途的決策，例如頁面邊距、正文、畫布底色 | 模板與元件 |
| 元件 | 某個元件必須偏離語意值時的別名 | 該元件 |

模板與元件引用語意 token。它們不寫原始色碼、不寫未經 token 的像素、不在斷點旁抄一份魔術數字。元件 token 必須指回語意 token。

間距是唯一的例外：元件內部的內距與間隔可以直接使用 `space.scale`，因為級距本身就是間距決策。頁面層級的間距，也就是邊距、欄距與區域間距，一律使用 `layout` 的語意 token。

數值格式：檔案宣告 `unit` 時，該檔的長度寫成數字，單位就是 `unit`；單位不同的長度寫成帶單位的字串，例如 `measure` 的 `68ch`。欄數這類計數沒有單位。沒有宣告 `unit` 的檔案，每個長度都寫成帶單位的字串。全域設定只寫一次，`rootPx` 只放在 `tokens/index.json`。

引用語法是 `{檔案id.物件路徑}`，而且必須是整個值。`color.contrast` 這類檢查表也用同一種語法指向 token。`color.semantic.text.primary` 這類路徑裡，每一段都是物件鍵，不使用把多個詞拼在同一個鍵裡的點號。

一個語意 token 可以按寬度帶給值，例如 `layout.margin`。消費端只寫 token 名稱，由解析順序選值。`base` 是缺省值；較寬的帶沒有寫值時，沿用下一個較窄的帶。

改原始 token 時，檢查所有指到它的語意 token。改斷點時，檢查每一個按寬度帶取值的語意 token。改完執行 `python3 scripts/check.py`，全部通過後，依 [GOVERNANCE.md](GOVERNANCE.md) 決定是否遞增 `tokens/index.json` 的 `version`。檢查包含：

- 引用能解析，而且沒有循環。
- `color.contrast` 全部達標。
- 原始色階的深淺方向正確。
- `layer.z` 嚴格遞增。
- 每個 token 都被其他 token 引用，或在本文件中以反引號提及。

沒有規則的 token 不應存在：新增 token 時，同時在本文件寫下它的用途。

## 6. 色彩與對比

`tokens/color.json` 的 `primitive` 是結構用色板，同一色相內數字越大越深；`semantic` 是介面實際使用的角色。主題是另一份語意指向，用來替換 `semantic` 的目標；主題不改元件規則，也不改斷點。本階段只提供一份預設指向。

對比要求寫在 `color.contrast`，單位是 WCAG 相對對比：

- 主要文字對畫布、表面、下沉底色至少 7:1。
- 次要文字、第三級文字、連結、狀態文字對其所在底色至少 4.5:1。
- 強調色上的文字至少 4.5:1，懸停狀態也一樣。
- 非文字元素至少 3:1，包括控制項的邊界、焦點環，以及唯一用來辨識狀態的圖形。

新增前景與底色的組合時，同時把它加進 `color.contrast`。

輸入框、核取方塊、選項按鈕這類控制項的邊界使用 `border.strong`。`border.subtle` 與 `border.default` 對畫布不到 3:1，只能用於裝飾性分隔，不能是辨識元件範圍的唯一線索。

狀態不只靠顏色。成功、警告、危險、資訊同時使用文字或圖形。

焦點使用 `focus.ring`，寬度與偏移見 `size.focus`，並且使用 `:focus-visible` 的時機，避免滑鼠點擊留下永遠存在的環。偏移必須大於 0，讓環與元件之間露出外側底色。焦點環的對比對象是環實際落在的底色，不是元件本身；環外側是 `bg.inverse` 或強調色時，改用 `focus.ringInverse`。

使用者要求更高對比時，第三級文字改用 `text.secondary`，裝飾性分隔改用 `border.strong`。其他語意 token 不變。

強制色彩模式由系統決定顏色。此時不得以背景色或陰影作為元件邊界的唯一線索：控制項與卡片保留 `size.border.hairline` 的透明邊框，讓系統有邊框可以上色。焦點環與選取狀態使用系統的強調色關鍵字，不寫死色值。

## 7. 元件契約

本階段不製作元件庫。之後每一個元件都要符合這份契約。

元件宣告內容槽、尺寸（compact、regular、comfortable）、是否依容器改變內部結構，以及它支援的狀態。狀態集合是：預設、懸停、按下、鍵盤焦點、選取、唯讀、停用、忙碌、錯誤。沒有該互動的元件可以省略不適用的狀態。

外觀約束：

- 懸停與焦點不改變元件佔位。
- 錯誤文案參與流動，把後續內容往下推。
- 停用的控制項不可聚焦、不可提交。
- 唯讀的控制項可以聚焦，也可以選取與複製內容，但不能修改。它的外觀和停用不同，文字維持正常對比。
- 選取包括勾選、目前所在頁與已選的列。選取不只靠顏色，同時使用勾號、粗邊框或文字標示。
- 忙碌的控制項保持聚焦，忽略重複啟動，並以文字或狀態呈現忙碌。
- 只在懸停時出現的操作，必須有不依賴懸停的到達方式。
- 截斷是元件的顯式選項，預設隨內容擴展。

元件內部若會從單欄變成多欄，必須寫容器帶，而不是讀視窗寬度。

## 8. 重排與輸入

寬度帶或短高度條件變化時，保留使用者已經產生的狀態：捲動位置、選取、輸入中的內容、展開中的對話框。重排不重複提交動作。

閱讀順序就是結構順序。寬度帶改變後，Tab 順序跟著新的視覺順序，兩者保持同一套順序。每個可互動表面在出現時有一個初始焦點位置；對話框把焦點放進自身，關閉後焦點回到開啟它的控制項。

鍵盤可以到達每一個操作。Enter 與 Space 的啟動行為與點擊相同。Escape 關閉最上層的暫時層。指標粗細改變時，目標尺寸立刻改用對應的 `target` 值。

軟鍵盤出現時，正在編輯的欄位與它的提交動作留在可見範圍內。欄位聚焦期間，單鍵捷徑不攔截文字輸入。

## 9. 層級、海拔與動效

疊層只使用 `layer.z` 的命名層，由低到高是 `z.base`、`sticky`、`dropdown`、`toast`、`scrim`、`modal`。元件不自訂插入中間的 z 值。

同一層內，後開啟的蓋住先開啟的。從某一層內開啟的暫時層，例如對話框裡的下拉或提示，使用開啟者所在的層，並依同層規則蓋在開啟者之上，不回到自己的預設層。

`scrim` 是對話框後方的遮罩。暫時訊息使用 `toast`，位於遮罩之下：對話框開啟時，訊息被遮罩壓住，不蓋住需要立即決定的對話框。對話框開啟期間必須讓使用者看到的訊息，放進對話框本身。

海拔見 `tokens/elevation.json`。平坦表面用 `flat`，抬起的卡片用 `raised`，下拉與提示用 `elevation.usage.dropdown`，居中對話框用 `dialog`。全螢幕對話框貼齊視窗，使用 `flat`。海拔表達層次，不拿來傳達狀態。

動效見 `tokens/motion.json`。時長依變化範圍選擇：

- `instant`：不需要過場的切換。
- `fast`：懸停、按下、焦點這類原地的外觀變化。
- `durationMs.base`：下拉、提示、訊息的進出。
- `slow`：對話框、全螢幕層與側邊面板的進出。

結構切換使用 `instant` 或 `fast`，完成前不阻擋輸入。位移只用於表達進出的暫時層。

曲線：進場用 `easing.enter`，退場用 `easing.exit`，留在畫面上的元素改變大小或位置時用 `easing.standard`。

使用者要求減少動效時，位移與縮放的時長改為 `reduced.durationMs`，也就是直接到終點。不造成位移的透明度變化可以保留，時長不超過 `reduced.opacityMs`。

## 10. 此階段不包含

- 品牌主題與第二套語意指向，包括深色模式。
- 任何具體頁面、導覽資訊架構或業務流程。
- 元件的視覺稿與程式實作。
- 國際化文案。規範只要求容器能隨文案變長，不在此凍結語言。

這些項目開始時，沿用本文件的解析順序與 token 分層，不平行再定義一套斷點。
